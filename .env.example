# V0-01 and the fixture-driven UI do not require external credentials.
# Populate a service only when exercising its live adapter.

OPENAI_API_KEY=
DATABASE_URL=
DIRECT_DATABASE_URL=
TEST_DATABASE_URL=
